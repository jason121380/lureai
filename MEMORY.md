# MEMORY.md — 決策與偏好紀錄

跨 session 的專案記憶。新的決策往上加，帶日期。

## 工作流程偏好（使用者指定）

- **每次 push 後一律開 PR 併入 main**（2026-08-31 授權），main 更新後 Zeabur 自動部署。
- 測試必須全綠才能 push；UI 改動用 Playwright 截圖驗證後再交付。
- Repo 已改名 `jason121380/lureai`（舊名 hair_brain 自動轉向）。

## 產品決策

- **2026-09-01 LINE 漏掉閒聊回覆**：後來新增的 `smalltalk` 模式（打招呼／道謝／情緒）不在 `/api/bot/reply` 的放行名單裡，導致設計師在群組說「hello」時大腦其實回了一句話，卻被當成降級丟掉、AI 完全不回話（後台測試聊天顯示「模型暫時不可用（used）」——連理由都是錯的，`used` 是模型成功的狀態）。放行名單改成 `llm`／`boundary`／`smalltalk`。教訓：新增回答模式時要一併檢查這條名單，它是白名單不是黑名單。

- **2026-09-01 LINE 回覆設定整組拿掉**（使用者：「lureai 是不是不需要這個設定」）：昨天才把設定收進後台，看了畫面才發現六個旋鈕裡有三個（語氣／長短／去標點）跟 `line` 語氣的內建規則重疊、會互相打架，剩下的停頓與拆則其實是通道機制不該給人調。結論是回覆行為完全照 `line` 語氣走：後台 `#linebot` 分頁、`/api/admin/bot-style` 與 `/api/bot/style` 全部刪除，`app/humanize.py` 只剩去引用／去標點／拆則（最多 3 則）／停頓 8-25 秒的固定動作。要改回覆行為就改 `line` 語氣或 `humanize.py`，不要再長出設定面板。

- **2026-09-01 LINE 回覆設定收進後台**（使用者：「設定就該跟大腦放一起，lurebot 不需要有」）：真人模擬設定改由後台 `#linebot` 分頁編輯（`GET`／`POST /api/admin/bot-style`），`/api/bot/style` 降為唯讀，lurebot 那邊的編輯介面拿掉、只剩唯讀顯示。順手修掉一個會讓 AI 在 LINE 靜默的地雷：`line` 語氣原本仍受引用守門，但編號在出口就會被剝掉，模型少寫一個 `[1]` 整則就被丟棄、AI 直接不回話——現在 `requires_citations` 同時放行 `service` 與 `line`。

- **2026-09-01 LINE 砍尾巴的 bug 在 humanize 改寫後回歸**：PR #55 把 `app/humanize.py` 整個重寫（拿掉可調設定），連帶把 `postprocess` 寫回 `parts[:MAX_PARTS]`，之前修好的「超過 3 則要併中間、不要砍尾巴」又不見了，回歸測試也一起被刪掉。已重新套用並補回 `test_postprocess_keeps_every_line_when_capping`。**重寫這個檔案時務必保留這個行為**：砍尾巴會讓收尾的問句直接消失，設計師在 LINE 收到的是一段沒講完的話。

- **2026-09-01 一則訊息可以有多行**（使用者：「一句回覆可以有多行」，示範「我想要吃／海鮮／玉米／薯條」）：原本每個 `\n` 都被切成一顆泡泡，所以列東西時會爆成好幾則。改成 **空一行＝換一則、單純換行＝同一則裡的下一行**（`serviceSentences` 與 `humanize.postprocess` 都改成用空行切、行內換行保留，前端用 `<br>` 渲染）。字數規則跟著改成「每一行 12 字以內」，範例改成「整段放同一則裡逐行往下寫」。超過 4 則時的合併也改用換行接，不會再併成一長條。

- **2026-09-01 第一輪先接住，不要一開口就給整套流程**（使用者附 LINE 截圖：他只說「廣告就成效不好」，AI 直接發三則排查流程叫他打開紀錄表、看觸及點擊私訊、把數字貼上來；他要的是「喔喔 沒關係／我幫你看一下」再「看哪邊可以優化」）：問題在於這句話**檢索得到知識**（ads-12 排查順序），所以模型把整套流程倒出來，先前的「開場先給安心感」規則管不到。新增 service-25：第一次丟出狀況又沒給細節時，第一則先接住並表示會幫忙，不要立刻列步驟、不要叫他打開表格、不要一次要好幾個數字；整套流程等他說要開始或已經給數字再拆。判斷方式寫成「這一輪想寫的超過 2 則就是給太多了」。順便把截圖裡的「請先打開紀錄表」加進用詞對照（→「幫我打開紀錄表」）。

- **2026-09-01 列項目要用講話的方式並附白話解釋**（使用者附截圖「先看私訊到店成效／記下曝光與點擊率／記下私訊預約到店／補上花費營收收益比」，示範改成「先看私訊到店率／有幾個來店」「再來要看 點擊率／看多少人點你的廣告」）：只有名詞的短句讀起來像表格不像人在講話。新增 service-24：每一項寫成兩行，第一行用講話的方式說要看什麼（用「先看…」「再來要看…」串順序、不要每行同一個動詞開頭），第二行用白話解釋那個數字的意思；同一項的兩行放同一則（只換行不空行）。

- **2026-09-01 開場先給安心感、每則之間要等**（使用者附 LINE 截圖與改寫示範）：新增用詞對照「先別急著解決全部 先讓我知道最卡的是哪一段」→「沒關係！我來幫你分析看看」、「你先回我其中一個就好 我陪你往下拆」→「你現在卡在哪個部分呢」，並明訂「開場先給安心感再問問題，不要先糾正對方」。標點規則放寬：**「！」可以用**（使用者示範裡有），其餘 `，。、？「」` 照舊不用。另外 `/api/bot/reply` 新增 `message_gaps`（每則之間 2~4 秒，可在校調頁「LINE 出口設定 → 每則之間的間隔秒數」改），lurebot 照秒數依序送，訊息才不會三則同時跳出來。

- **2026-09-01 泡泡長度再收一輪**（使用者連兩次回報，第二次附截圖：話術那一則 80 字塞成一顆）：元凶是我自己寫的例外「只有範例那一則可以超過 12 字」。移除例外——**範例與話術也要拆成 12 字以內的短句**，並要求給範例時整則回覆就只有範例＋一句問句、不要再寫開場白，這樣才塞得進上限。上限同時從 3 則放寬到 **4 則**（使用者：「講不完可以分句 3-4 句最多」），前端 `SERVICE_MAX_BUBBLES` 與 `humanize.MAX_PARTS` 一起改，超過時仍是併中間、不砍尾巴。另外把「絕對不要把一句話切到一半」放寬成「一句話講不完可以在語意停頓處分成兩則」（使用者：「回覆可以單句內分段」），並新增用詞對照「請你拍 3 張作品照」→「幫我拍 3 張作品照」（要他做事用「幫我…」不用「請你…」）。

- **2026-09-01 使用者實測回報三件事，逐一查證**：
  (1) **自我介紹掉 fallback**——使用者判斷「是後端分流在決定，改 prompt 沒用」，**判斷正確**：`我叫小婷 在板橋做三年` 超過 12 字又不是招呼語，直接進 RAG、撈不到就回「我手邊的資料不夠」。新增 `SELF_INTRO_PATTERNS` 分流（我叫X／我是X設計師／我在X店／做N年），並用 `speaker_name()` 從歷史撈出名字，透過 `AnswerEngine.speaker_note()` 帶進指令，之後可以直接叫他名字。
  (2) **話術不帶標點**——使用者以為是「不用標點那條在組裝時蓋掉例外」，**實際根因不同**：前端 `serviceSentences` 的正規表示式無差別剝掉 `，。「」`，改 prompt 一定沒用。我一度做了「引號裡面保留標點」的例外，使用者澄清「我就是不要 ，。「」」後整個拿掉，維持完全不要標點。所以這件事的本質是 (3) 的儲存 bug——他改的 service-12／22 根本沒存進去。
  (3) **後台儲存按鈕存到 policy-00**——**我用新版與他當時那版（`0954854`）各跑一次 Playwright 攔封包都無法重現**，兩版都送對 rule_id。沒有當作沒發生：把 `saveRule` 從全域 `document.querySelector('[data-text=...]')` 改成**只從被點的那張卡片內部取值**，並加上 `field.dataset.text !== ruleId` 的防呆與「已存檔：<規則名>」的 toast，讓萬一再存錯當下就看得見。根因（快取到舊 JS／部署中新舊檔混搭）仍未確認。

- **2026-09-01 規則正本只有一份**：改 (2) 時發現我改了 `app/answer.py` 的 `TONE_INSTRUCTIONS`，但實際送出的是 `tuning.compose_tone()`——改了不會生效，是我在 PR #58 留下的地雷（測試抓到了）。已把 `TONE_INSTRUCTIONS` 改成由 `tuning` 組出來的衍生值，兩份不可能再走音。**以後改規則一律改 `tuning.py` 或後台。**

- **2026-09-01 客服單則字數 20 → 12 字**（使用者：「回覆長度怎麼改，都太長了，要 12 字以下最好」，附一則 60 字的泡泡）：規則改成「每則 12 字以內、一則就是一句話——報價一則、時段一則、提醒一則，不要用空白接成一長串」，並要求寫完自己數一次字。範例那一則仍是唯一例外。

- **2026-09-01 對話紀錄改存資料庫**（使用者：「為什麼我用手機登入同個帳號，看不到我的對話紀錄」→「聊天內容當然要存資料庫，還有哪些該存得，都存起來」）：原本對話只存 localStorage、伺服器完全不存聊天內容，所以換裝置就看不到。盤點後只有兩樣東西是瀏覽器獨有的，兩樣都搬進 SQLite 並加進 `DURABLE_TABLES`（Postgres 快照）——`conversations`（per user id，含標題、語氣、訊息）與 `user_prefs`（目前只有語氣，白名單擋前端亂塞）。前端改成登入時先拉伺服器那份合併（伺服器為主、本機獨有的留著），localStorage 降為離線快取，編輯後 1.2 秒 debounce 上傳；**舊使用者只在瀏覽器裡的紀錄會在第一次登入時自動搬上去**。上限：每帳號 100 段、每段 200 則、單則 2 萬字，超過會刪最舊的。存之前會把 `loading`／`pendingReveal` 清掉，否則重新載入會一直轉圈。實測兩個瀏覽器 context 登入同一帳號看得到同一份紀錄。**這推翻了原本「伺服器不存聊天內容」的隱私設計，是使用者明確決定的。**

- **2026-09-01 校調頁改成左選單右內容**（使用者：「這邊東西很多，改成左側選單、右側內容」）：51 條規則排成一長串捲不完。左側 208px sticky 分組選單（顯示每組條數，改過的顯示「N 改過」），右側只渲染該組；手機版選單變成可橫向捲的藥丸列。

- **2026-09-01 閒聊與情緒不進檢索**（使用者：「我覺得你回話太嚴格了，there's no small talk，要有 small talk，問到關鍵問題才是從 rag 拿」；隨後的健檢報告 D-2／A-3 也點名同一件事）：一句「哈囉」「謝謝」「算了 沒事」原本都會進檢索，撈不到就回「我手邊的資料不夠」。現在三類話在檢索前就被攔下來——打招呼／道謝／應聲、抒發情緒、欲言又止——交給模型自然接一句，不掛來源。**情緒句特別重要**：報告實測「這個時段我本來可以接別的客人 有點不爽」被同理一句之後又接失敗訊息索取數字，等於前功盡棄，所以情緒分支明訂只承接、不派任務、不要數字、不給二選一、不附追問。分流的關鍵是「有沒有在問問題」：句子裡有「怎麼／該不該／什麼／嗎／幫我」就讓路給 RAG（「我好累 該怎麼調整」要給方法）。判斷寫得很緊：招呼語要整句相符（用「包含」比對會把「好累」當成「好」），訊息超過 12 字一律當問題。

- **2026-09-01 LINE 回覆停頓回到校調頁**（使用者：「這邊沒有回覆時長」）：PR #55 把 LINE 設定整組拿掉時，停頓秒數被寫死成 `DELAY_RANGE = (8, 25)`，後台就沒得調了。改成校調頁的「LINE 出口設定 → 回覆停頓秒數」，格式「最短-最長」，`tuning.parse_delay_range` 會夾在 30 秒內（LINE 的 reply token 只有 60 秒），看不懂就用預設。

- **2026-09-01 後台新增「AI 模型校調」**（使用者：「把從以前到現在所有給你的規則列出來，至少我看得到，並且可以修改，改完後你變成 ai 看得懂的」）：規則本來散在三處——`config/designer_coach_policy.md`、`app/answer.py` 的 `TONE_INSTRUCTIONS`、政策引擎的固定回覆句，使用者看不到也改不動。新增 `app/tuning.py` 把它們收成一份 44 條的目錄（基本規則 5 段＋專家 1＋客服 23＋LINE 8＋固定回覆句 7），後台 `#tuning` 逐條顯示與編輯，改過的存進 `model_rules` 表（已加進 `DURABLE_TABLES`，重新部署不會掉），沒改的沿用預設。`AnswerEngine.instructions()` 與 `PolicyEngine` 每次都重讀覆寫，所以存檔後下一則回答就生效、不用重啟。「看實際指令」按鈕會顯示三種語氣各自組好、真正送給模型的整段文字。**關鍵安全網**：`tests/test_tuning.py` 驗證沒有任何覆寫時 `compose_policy()`／`compose_tone()` 跟原本逐字相同，所以這次重構沒有改變任何 AI 行為。以後改規則走後台或改 `tuning.py` 的預設值，不要再直接改 `TONE_INSTRUCTIONS`。

- **2026-09-01 客服單則字數失控**（使用者貼出一顆 46 字的泡泡：「我們先記 1 週每天預計下班 實際下班和晚下來的原因 你比較像客人拖延 還是下班後事情太多～」）：一則裡塞了「要記什麼」「怎麼記」「二選一問句」三件事。原本的規則是「盡量精短（15 字內為佳）」太軟，而且剛加的「範例那一則可以長」被過度套用。改成硬上限 20 字（只有範例例外）、一則只講一件事、最後一則單獨放二選一問句不要黏在句尾，並把這句實際的長句當反例寫進指令。**沒有在前端加字數硬拆**：試算過greedy 依空白切這句會變成「實際下班和晚下來的原因 你比較像客人拖延」「還是下班後事情太多～」——二選一被切成兩半，比長句更糟；只有模型知道語意邊界，所以長短一律靠指令控制（使用者先前也否決過強制斷字）。

- **2026-09-01 後台 UI 比照前台整體優化**（使用者：「後台 UI 大小比例配色間距 比照前台做整體優化」）：六個分頁逐一截圖比對後改了五類問題——(1) 內容沒有寬度上限，1440px 滿版時健康檢查的數值離它的標籤超過 1000px，加 `--admin-content: 1120px` 置中；(2) 字級失控，9~11px 散落各處（健康檢查的 meta 只有 9px），全面拉到「主體 13／說明 12／mono 11」，區塊標題 18→20px；(3) `待整理 0` 永遠是紅字，改成只有 `> 0` 才上警示色；(4) 分類卡每格都描框，40 幾格時框線比內容搶眼，改成 `--bubble` 淡底；(5) LINE 設定三個下拉沿用 `2fr 1.2fr 1fr` 導致寬度不一，加 `.editor-row.even-row` 等寬。另外健康檢查列從三欄改兩欄、耗時移到狀態徽章旁邊，手機版數值換行靠左。實測桌機 1440×950 六個分頁與手機 390×844 都沒有水平溢出。

- **2026-09-01 客服模式「答案不到位」的真因是前端吃掉正文**（使用者貼出「可以先用這個貼文範例／最近想整理髮型的你／我可以陪你改成更像你的版本」——範例只剩一句開頭）：`serviceSentences` 的 3 則硬上限實作是 `[lines[0], lines[1], lines[最後一則]]`，超過就把**中間整段丟掉**，所以範例／話術的正文會憑空消失，只剩開頭跟結尾。改成「頭尾照留、中間併成一則」，內容一個字都不會少，畫面仍維持 3 顆泡泡。`app/humanize.py`（LINE 路徑）有同一類 bug——`parts[:MAX_PARTS]` 是砍尾巴，連收尾的問句都會不見，一併改成同樣的併法。另外在客服指令補上「提到範例／話術／模板／文案就要給完整可直接複製的內容，那一則可以長、不要拆」。

- **2026-09-01 語尾助詞要看句型**（使用者：「還是髮質整理的貼文呀／或是髮質整理的貼文～，要判斷在正確的 context 出現」）：原本只寫「適度加唷／呀／～」，模型會隨便代換。改成明確分工——「唷」只放提醒叮嚀的陳述句、「～」放問句（特別是二選一與反問）、「呀」放接住情緒或輕聲確認的句子；講數字步驟條件時不加助詞；一則最多一個、不疊用「唷～」。

- **2026-09-01 不要講出系統的運作方式**（使用者：「換個問法可能就有了～你可以直接點下面的題目，不可以這樣講，這樣就知道你是 bot」）：查無資料的固定說法原本會叫使用者換問法、點題目按鈕，等於把介面與檢索機制講出來。`config/designer_coach_policy.md` 規則 13 改成「這題我手邊的資料不夠 沒辦法給你準的答案 你方便跟我說一下你現在的數字嗎」，並新增 13-1 明列禁語（換個問法／點下面的題目／知識庫沒有／我的資料庫／重新輸入／我查一下系統）；`app/policy.py` 的預設 `FALLBACK_MESSAGE` 也不再提「知識庫」。注意：被直接問「你是不是 AI」時仍照實回答（`BOUNDARY_REPLIES` 的 identity 不變），這條規則只針對漏出介面機制。

- **2026-09-01 空白頁標題貼著輸入框**（使用者：「太靠下方的輸入筐」）：標題吃 `.welcome { padding-top: min(28vh,310px) }`、輸入框吃 `.is-empty .composer-shell { top: 42% }`，兩套單位各算各的，視窗一矮就交叉——實測 1440×700 時標題與輸入框**重疊 5px**，882 高也只剩 21px 間距。改成三者共用 `:root` 變數（`--topbar-h`／`--welcome-top`／`--welcome-title-h`／`--welcome-gap`／`--welcome-composer-h`），輸入框的 top 由「topbar＋welcome-top＋標題高＋間距」推出、建議題目的 margin 是「間距×2＋輸入框高」，輸入框正好浮在中間。手機版只覆寫變數（`--topbar-h` 含安全區）。實測 1512×882／1280×900／1440×700／390×844 的間距都穩定在 41/39（手機 35/31），沒有溢出。

- **2026-09-01 確認彈窗兩顆按鈕形狀要一致**（使用者：「形狀都要一樣 以黑色為主」）：`.tone-ok` 同時掛 `command-button`，而 `.command-button` 在樣式表更後面，圓角（藥丸）與 `min-height: 40px` 會蓋掉 `.tone-ok` 的 10px／32px，所以取消是圓角方塊、切換是藥丸。改用 `.tone-confirm-actions .tone-cancel/.tone-ok` 這種更明確的選擇器統一成 34px 高的藥丸，切換黑底白字、取消白底黑框黑字。

- **2026-09-01 開場建議 100 題、每次抽 5**（使用者要求「建議訊息到 5 則、每次都不一樣、randomize 100 個」）：`run.py` 題庫 24 → 100 題（不重複），涵蓋私訊對話健檢、廣告、社群、輔導流程、設計師人生題與店務營運六大塊；每題都先跑過檢索驗證（policy 放行＋top1 分數 ≥ 0.72，138 個候選全數通過後平均取樣成 100）。`/api/health` 改回傳整份打散的題庫（`WELCOME_PROMPT_POOL`），前端每次渲染空白對話再抽 5 題（`WELCOME_PROMPT_COUNT`）。桌機排成 2 列、手機 5 列，實測都不撐破版面。加題時務必先驗分數，`tests/test_welcome_prompts.py` 會逐題擋。

- **2026-09-01 客服模式不做引用守門**（使用者回報正常回覆卻跳出「這題我剛剛沒有整理好」）：根因是設計自相矛盾——客服指令要求口語短句、前端又會把 `[n]` 剝掉，但守門仍硬性要求整篇至少一個 `[n]`，模型照指令寫出來的好答案（例：「我幫你看一下唷～／私訊盡量 2 小時內回／你想先調速度還是先看內容～」）被丟掉、重試再丟掉，使用者只看到降級訊息。修法：新增 `AnswerEngine.requires_citations(tone)`，客服模式直接放行（`app/answer.py` 與 `app/service.py` 串流路徑同步），並把客服指令裡的「要寫編號」改成「不用寫編號，但內容只能出自來源」。專家模式守門不變。

- **2026-09-01 lurebot 的 AI 大腦搬到 lureai**：LINE 端不再自己拼知識（Gemini + SOP/FAQ + 上傳文件那套全部拆掉），改打 lureai 的 `POST /api/bot/reply`。分工是「lureai 是大腦、lurebot 是通道」：群組脈絡（群組名／發話設計師／輔導階段）由 lurebot 隨請求帶上，知識、政策、生成、引用守門、稽核都在 lureai；真人模擬（語氣／長短／去標點／拆則／回覆停頓）也一起搬進來（`app/humanize.py`），所以大腦直接回「拆好的多則訊息 + 建議停頓秒數」，lurebot 只負責等秒數與送出。新增 `line` 語氣、`X-Bot-Token` 服務權杖與 `lurebot` 服務帳號（用量與月預算記在它頭上）。LINE 的 reply token 只有 60 秒，所以停頓上限抓 30 秒，lurebot 端還會再夾一次剩餘效期。

- **2026-09-01 header 被捲掉（版面骨架）**：使用者回報對話一多 header 就不見，並自己抓到根因——`.app-shell` 的 grid 只定了 columns，隱含的 row 是 `auto`，不受 `height:100%` 限制；20 段對話時側欄內容 1187px > 視窗 824px，把整個 grid 撐大，`<html>` 開始整頁捲動（body 有 `overflow:hidden`、html 沒有），`position:relative` 的 topbar 被捲出畫面（實測 scrollY 363、headerTop −363）。修法：`.app-shell` 補 `grid-template-rows: minmax(0,1fr)` 與 `overflow:hidden`、`.sidebar` 補 `min-height:0`、`html` 也加 `overflow:hidden`。修後 1187→824、scrollY 0、headerTop 0。教訓：我第一次只用「一段長對話」測所以沒重現——版面題要用「側欄很多筆」而不是「訊息很多則」來測。已加 `test_app_shell_row_cannot_be_stretched_by_the_sidebar` 防回歸。

- **2026-09-01 補上「設計師人生題」知識（健檢報告 P1-1）**：新增 `knowledge/career_playbook.md`（career-01~32，domain=coaching），補齊報告中失敗率 60~80% 的七個缺口——開店評估與損益兩平、工時與晚下班、界線與拒絕客人、休假與產能、職涯低潮與業績挫折、店內客人歸屬與同事衝突、定價調整幅度（一次 10% 上下、不要一次漲 200）、訂金金額與話術、進修投資回本、產品銷售的具體開口句。同步補 32 組問法種子。索引 209→241 塊、問法 1.2 萬→1.42 萬；覆蓋率 89.3%（原 88%）、100% 過門檻；報告裡 16 題實測全部命中正確知識塊。

- **2026-09-01 健檢報告 P0 三項修復**（使用者做了 20 段 × 5 輪的實測報告，失敗率 31%）：
  (1) **不再傾倒知識原文**——生成失敗時原本會丟 300~1500 字不相干原文並外露 `missing_citations`，改成一句誠實的話＋一個小問題（`AnswerEngine.MODEL_FAILED_MESSAGE`），降級時 `citations` 也清空，前端狀態碼只放進 title。
  (2) **串流先送 header**——`chat_stream` 在驗證後、檢索前先 yield `{"type":"start"}`，伺服器才能立刻送出 header 與第一批位元組；原本要等整個檢索＋模型首個 token 才送 header，閘道等不到位元組就切斷（正式站間歇 503 的真因）。前端連線失敗時保留使用者輸入並給「重送這則」按鈕。
  (3) **邊界守門**——`app/policy.py` `BOUNDARY_REPLIES` 在檢索前攔下四類非輔導題（離題／不當請求／問身分／情緒挑釁），直接給固定回應、不進 RAG、不掛來源、狀態列顯示「這題不在輔導範圍」。
  另外：答不出來時的建議追問改用驗證過的開場題庫（原本會冒出「毛髮構造」這種不相干題目）；客服指令新增「不承諾做不到的事」（不說我幫你看數字）、情緒句先接住、說了「你可以說」就一定要接話術。
  尚未處理（報告 P1-1）：開店、工時、拒絕客人、休假、職涯低潮、客人歸屬、訂金金額等「設計師人生題」知識缺口。

- **2026-09-01 品牌資產更新**：使用者把正式 logo.png（15216px 寬）與 favicon.png（9419px 方形）直接上傳到 GitHub repo 根目錄——已縮成網頁尺寸搬進 `static/`：logo.png 800px 寬（透明底，`logo.svg` 已刪除、頁面與 sw.js 改引用 logo.png、cache 版本升 v2）、favicon 256、app-icon 512／192、maskable 512（80% 安全邊，白底攤平避免 iOS 透明變黑）。`app/health.py` 前端檢查改支援二進位資產（PNG 驗簽章，不做文字比對）。iOS 要刪掉主畫面圖示重新加入才會看到新 icon。

- **2026-09-01 右上角回覆模式標示**：topbar 右側常駐顯示「專家模式／客服模式」膠囊（`#tone-indicator`），`setTone` 同步更新文字。點擊行為後改為：跳出小彈窗「是否切換為◯◯模式？」（取消／切換），確認才切換；點外面或 Escape 關閉。帳號彈窗的設定分頁仍是另一個切換入口。

- **2026-09-01 對話清單帶時間**：每筆對話顯示最後活動的日期時間（`updatedAt`，舊資料退回 `createdAt`；格式 M/D HH:mm 等寬字），有新訊息的對話自動排到最上面。

- **2026-09-01 介面再減法**：移除輸入框下方整排 meta（「回答僅使用已核准知識」與字數統計 0/1200），使用者要求畫面更乾淨。

- **2026-09-01 後台知識庫「滑到底就沒了」＋切換卡頓**：(1) `/api/admin/chunks` 的 200 筆雙重上限（查詢 limit＋回應切片）吃掉 209 塊中的最後 9 塊——改列出全部，頂端顯示「共 N 則」。(2) 全列之後切換主題／來源每次重打 API 出現「載入中」卡頓——改成整包只抓一次進記憶體、切換用前端過濾即時渲染；搜尋仍走伺服器檢索；新增／編輯／刪除／重建索引後強制重抓（`loadKnowledge(undefined, {force:true})`）。

- **2026-09-01 客服模式真人化（使用者細部規格）**：專業穩重親切、語尾不用「啦」可用「唷」；每則訊息精短並**依語意斷句**（原本的 10 字硬拆會把「2 個選/擇」切壞，使用者要求取消——前端不做字數硬拆，模型每行＝一則訊息，指令要求 15 字內為佳、絕不切半句）；不用任何標點符號、以空白分段；數字一律阿拉伯數字。回覆像真人打字：第一句直接出現，之後每句停 1~2 秒（`pendingReveal`＋輸入中點點），狀態列／來源／追問全部發完才顯示；串流期間不吐字。逐句泡泡只在模型成功回覆（model_status=used）時啟用，降級知識原文維持一般排版。

- **2026-09-01 帳號彈窗（左下角→置中卡片）**：語氣切換與本月用量移進「按名字打開」的彈窗。第一版是貼著帳號列的小面板，使用者改要求「彈窗卡片＋二分欄」：現在是置中 560px 卡片（壓暗背板），左欄導覽（設定／用量，含 icon）、右欄內容；手機版左欄縮到 96px。背板、X、Escape 都會關閉；打開時順手刷新用量。側欄不再常駐用量卡。帳號列 hover 分三區：名字（`.account-trigger`，開彈窗）、設定、登出各自獨立，不再整列反白。

- **2026-09-01 回答評分（👍👎）**：每則回答下方有 thumbs up/down，POST `/api/feedback`（登入限定）存 SQLite `feedback` 表（同一人同一則只留一票，重按改票）；`GET /api/admin/feedback` 可拉回饋清單（JOIN 稽核帶出問題與分數），供之後挑出要加強的知識。前端評分同時存 localStorage 顯示選取狀態。

- **2026-09-01 介面細修一批**：(1) 引用區加「知識來源：」標籤、編號籤縮小（25px 高、15px 圓號）；(2) 空白頁輸入框縮到 640px（手機 400px）、輸入列永遠不加陰影（含 focus，只用邊框變深提示）；(3) 「新對話」不重複疊空白對話——已有空的就切過去，按鈕也拿掉常駐底色；(4) 刪除對話不用瀏覽器原生 confirm，改頁內兩段式（按一下變「確定刪除」，3 秒內再按才刪）。

- **2026-09-01 客服語氣加溫＋引導式**：使用者兩次回饋——(1)「太冰冷」：指令加上「溫暖有人味」，句尾適度加「唷」「呀」「～」（隔兩三句一次），仍然不用「啦」；「～」列為允許符號，前端去標點清單也放行。(2)「一次給太滿資訊」：改引導式對話，一次只推進一步——2~4 行、先接住狀況、最多 1 個重點、最後一行必用問題把球丟回去（優先二選一或只要 1 個數字），絕不一口氣列出所有要的資料。(3) 要有第一人稱：用「我」跟「你」對話（「我幫你看」），不用沒有主詞的說明句。(4) 不能太強勢：邀請代替命令（「方便跟我說…嗎」不說「你先回我」），不用「我教你」改「我們一起試」。(5) 用詞對照＋更短：「先別急著加預算」→「先不用增加預算」、「先看這次收益比」→「我們先看下投報率」、「抽 20 則對話做同一套評分」→「抓 20 個對話來分析下」、「看出最常卡住的位置再只改 1 件事唷」→「看哪邊卡住」；講重點不塞細節（不把六面向全列）；私訊數／預約數這種看得到的數據不問他、改「我幫你看一下唷～」，反問只用在二選一；每次最多 2 則、絕不超過 3 則——前端也做硬截斷（保留前兩句＋收尾問句）。

- **2026-09-01 語氣設定（專家／客服）**：輸入框下方新增語氣切換（localStorage 依使用者記住，空白對話也看得到、置中顯示）。專家模式＝預設，放寬長度規則（3~5 點、每點可 60 字、全篇 400 字），每點要講「為什麼＋怎麼驗收」；客服模式＝像真人在通訊軟體聊天：模型一行一句（3~6 行口語短句、禁止條列），前端把每行畫成獨立訊息泡泡（`.message-text.bubbles .chat-line`）。`tone` 隨 `/api/chat(/stream)` 傳入、未知值一律當 expert；引用守門與追問規劃完全不變。客服模式的引用只給系統核對：模型照樣行尾附 `[n]`，但前端渲染泡泡時剝掉編號（使用者要求句尾乾淨），來源照列在泡泡下方。

- **2026-08-31 手機版選單純白抽屜**（PWA 展開選單時頂端出現陰影與白色交界）：手機版側欄底色 `--surface` 純白、不壓暗背景也不加陰影——任何壓暗或陰影都會在狀態列下緣形成可見交界。2026-09-01 依使用者要求從全螢幕改為「左側 80% 寬」：右邊露出的 20% 用透明 `drawer-overlay.clear` 接住點擊關閉（仍然不壓暗），來源抽屜維持原本壓暗 overlay。

- **2026-08-31 PWA 淺色鎖定**（使用者要求狀態列全白、無分割線）：`theme-color` meta 三份（light media／dark media／無 media）全部 `#ffffff`，再加 `<meta name="color-scheme" content="light">` 與 CSS `:root { color-scheme: light; }`，避免系統深色模式把狀態列或捲軸染色。注意：iOS 在「加入主畫面」當下快取 theme-color，改版後既有使用者要刪掉圖示重新加入才會生效。

- **2026-08-31 開場建議隨機**（使用者要求「每次建議問題都要不一樣」）：`run.py` 內建 24 題開場題庫（每題經 `tests/test_welcome_prompts.py` 逐題驗證答得出來），`/api/health` 回傳隨機打散的 12 題（`welcome_questions`，題庫不足時從問法索引跨主題輪流補），前端每次渲染空白對話再從中抽 3 題。

- **2026-08-31 模型降級可觀測＋檢索先用問題本身**（使用者兩度回報「模型無法生成」查不出原因）：(1) `app/answer.py` 新增 `log_model_failure(stage, error, detail)`，LLM 失敗原因寫 stderr（不含金鑰與問題內容），Zeabur Log 搜 `[llm]` 即可定位；前端狀態列顯示降級原因標籤（如 `missing_citations`）。(2) 檢索改成先只用當題問題，top-1 低於門檻才補前兩題使用者問題重試並取較好者——修正短追問（「客人沒回」）被歷史脈絡拖去引用不相干知識。

- **2026-08-31 missing_citations 三重防護**（正式環境模型常「未回應」，狀態列顯示 missing_citations——模型有回但沒附 [n] 引用被整篇丟棄）：(1) 全形引用視同有效，【1】（1）〔１〕正規化成 [1]；(2) 缺引用時自動帶警語重試一次（串流與非串流路徑都有，用量兩次都記帳）；(3) policy 輸出格式明定「每點結尾附半形 [1]，沒有引用整篇作廢」。

- **2026-08-31 部署韌性**（使用者回報「進不去，一直轉」）：三個會讓整站打不開的地雷——(1) Dockerfile 沒指定 host，`APP_HOST` 一旦沒設就只綁 127.0.0.1，平台流量進不來；(2) `CMD` 用 `reindex-only && exec`，索引重建失敗（例如 Volume 唯讀）容器直接結束；(3) 少設 `ADMIN_TOKEN` 會讓 `run.py` 直接 exit 1。現在：Dockerfile `ENV APP_HOST=0.0.0.0`、重建失敗只記錄不中斷、沒有 ADMIN_TOKEN 就改用隨機權杖（後台本來就走帳號登入）。開機會印 `[boot] profile=… chunks=… db=… model=…`，Zeabur Log 一眼看得出卡在哪。前端也加了開機逾時，伺服器沒回應時顯示錯誤而不是一直轉。

- **2026-08-31 追問 50 輪不斷線**（使用者硬性要求）：建議問題改由 `app/followups.py` 規劃——模型寫的追問先驗證（政策不擋且檢索分數過門檻），不夠就從相鄰知識（同分類→同手冊→同主題→全庫，每輪旋轉一格避免繞圈）補到三個。前端改送最近 60 題（最後 8 題全文，其餘截 80 字）供去重。實測 8 條追問鏈都能連跑 80 輪不重複、不轉人工；`tests/test_followup_chain.py` 固定驗 50 輪。
- **2026-08-31 敏感話題重新界定**：原本的攔截清單是客服版遺留（多少錢／價格／優惠／薪資／抽成／請假／過敏／掉髮全擋），對設計師輔導反而幫倒忙——「客人嫌貴怎麼回」「廣告要投多少錢」都被轉人工。現在只擋「只有人能決定或有法律責任」的事：退費賠償提告、資遣解雇勞資爭議、就醫診斷用藥、顧客個資與付款資料、替客人現場預約。

- **2026-08-31 知識庫擴充到 209 塊＋1.2 萬問法**：使用者回報「資料庫一下就沒料」。實測 20 題有 2 題落在門檻外、5 題撈到不相干知識，根因是輔導知識只有 44 塊。新增四本手冊：`messaging_audit_playbook.md`（對話健檢與成交 30 塊：六面向健檢、回覆速度／長度／親切度／Emoji／專業度、二選一提問法、報價與追蹤）、`ads_playbook.md`（投放 23 塊）、`social_playbook.md`（社群版面 25 塊）、`session_playbook.md`（輔導流程 16 塊），並補 coach-42~44。
- **2026-08-31 問法索引（aliases）**：`config/question_bank.json` 手寫 329 個真實問法種子，編譯時用模板展開成 12,337 筆，寫進 `chunks.aliases` 與 `search_text`；檢索加一個上限 0.20 的別名加分，且必須同時命中標題或內文才算（避免任何問題都被拉高）。使用者要的「上萬則 QA」是這一層：上萬個問法對應 209 塊知識，不是上萬個手寫答案。
- **2026-08-31 分數校準**：全部知識都策展後，`curated_score` 對每塊都一樣只會整體墊高，降為 0.06；並把「麼辦／麼算／要不」這類跨字 bigram 加入 GENERIC_QUERY_TOKENS。覆蓋率 88%（top-3）、100% 過門檻。已知限制：純字面比對仍會讓「女朋友生氣」這種題目對到客訴知識，靠 policy 的接地規則擋。
- **2026-08-31 編譯統一**：`scripts/build_knowledge_index.py` 取代兩支舊 builder，一次編譯六本手冊；`test_written_index_matches_the_playbooks` 會擋住「改了 md 沒重編」。

- **2026-08-31 回答格式**：一句結論 → 2~4 個以動詞開頭的行動條列（每點 40 字內）→ 最多一句收尾，全篇 180 字內；禁止開場白、複述問題、表格與多層縮排。policy 新增「輸出格式」章節，前端 `renderAssistantMarkup` 改成會把 `- `／`1.` 轉成真正的 <ul>/<ol>（assistant 泡泡加 `.rich` 取消 pre-wrap）。

- **2026-08-31 來源全部重點整理**：1,080 塊原始教材（儲存格傾印 `A1=`、空白表單、公式、逐字 OCR）不再進索引，改由人工整理成 `knowledge/salon_operations_playbook.md` 的 71 塊重點知識（接待、售後、客訴、管理、財務、商圈、技術）；輔導手冊同時補 coach-42~44（投放收益比、網路行銷三要件、導客的目的）。索引從 1,124 塊降到 115 塊、2.6 萬字，全部是完整句子，品質旗標 0，33 題實測 31 題命中預期知識。原始語料封存在 `knowledge/archive/legacy_source_documents.jsonl.gz`。整理過程也清掉原本漏遮的主管姓名（教材裡的經理／處長名字）。

- **2026-08-31 知識庫兩大主題**：全部知識切成「店務營運管理」1,080 塊（店務營運／企業知識／顧客服務／人才與管理／業績管理／美髮技術）與「設計師一對一行銷輔導」44 塊（41 塊策展 coach 知識 + 數位行銷教材）。`domain` 寫進 JSONL 與 SQLite（含既有 DB 的欄位遷移），後台總覽依主題分組、知識庫可依主題篩選、新增知識要選主題。歸類規則集中在 `app/domains.py`，資料列自帶 `domain` 優先。

- **2026-08-31 視角統一**：所有回答、稱呼、追問一律以「你」直接對設計師本人說話，禁止第三人稱「設計師應該…」。policy、歡迎問題、追問指令與知識來源文字全部改寫。
- **2026-08-31 歷史教材定位**：門市教材屬「店務營運與服務 SOP」，與輔導知識是兩個主題；門市類問題多半靠它回答，故保留主題但改為重點整理版（見上方 2026-08-31 來源全部重點整理）。

- **2026-08-31 知識庫大整理**：移除 490 個對話逐字稿與 788 個 OCR 碎片，改由原始對話萃取成 41 塊乾淨策展知識（coach-01~41）。實測 23/23 題可回答，其中 20 題由策展知識回答。原始 JSON（270 對話 12,873 則）中僅 1,810 則為教練實質建議，其餘為數據回報樣板與短句。
- **2026-08-31 檢索三修**：(1) `config/synonyms.json` 同義詞層——原本「一週幾則」與「每週發布頻率」字面零交集完全撈不到；(2) 策展知識加權 +0.10 與標題聚焦分，避免短 SOP 被長逐字稿壓過；(3) 分數改用單調壓縮（0.5+0.5(1-e^-2.63x)）取代硬截斷，修正 41 塊同時撞 1.0 導致排序退化成字母序。
- **2026-08-31 後台改為知識編輯台**：移除檢索測試與查詢稽核；改為 總覽／知識庫／品質檢查／帳號／系統健康。目的是讓回覆資料整齊、多元、完整、可擴充、有條理。

- **2026-08-31 移除客服版**：只保留 `designer_coach` profile；客服知識檔、customer_policy.md、start.command 已刪除，預設 profile 改為 designer_coach。
- **2026-08-31 關聯問題**：模型在同一次生成結尾以「▷ 」行輸出 3 個追問，service 解析為 `followups`，前端顯示為可點選項。
- **2026-08-31 輸出無上限**：預設不設 max_output_tokens（`LLM_MAX_OUTPUT_TOKENS` 可選擇性限制）。

- **2026-08-31 統一登入**：廢除管理權杖登入頁。所有人走同一個帳號登入；admin 角色帳號在側欄看到「設定」icon（僅 admin 可見）→ 進 `/admin`；非 admin 開 `/admin` 直接導回 `/`。`ADMIN_TOKEN` 只保留給 API header（curl／測試／緊急）。
- **2026-08-31 角色制**：帳號分「一般用戶 user／管理者 admin」；後台建帳號時選權限；`USER_ROLE` 可指定 bootstrap 帳號權限。
- **2026-08-31 密碼門檻**：最低 4 個字（使用者要求，接受安全取捨；scrypt + 登入限流仍在）。
- **2026-08-31 全站視覺**：複製 ChatGPT 風格（見 STYLE.md）。對話：使用者訊息右側灰氣泡、AI 左側純文字、無頭像無角色標籤。
- 移除項目：側欄使用者名下的應用名稱副標、右上角連線狀態圈圈、後台頂部「知識庫管理＋管理權杖」列、後台的權杖輸入 UI。
- 用量面板做成獨立卡片，與帳號列視覺分離。
- 後台為分頁式導覽（hash 路由），進入後所有分頁資料自動載入，不需任何手動觸發。

## 技術決策

- 零第三方依賴維持不變（stdlib + SQLite FTS5），唯一例外：`psycopg`（僅 Dockerfile 安裝、只在設 Postgres 連線時 import）。
- **2026-09-01 持久化改 Postgres 快照（使用者明確不要 Volume）**：SQLite 仍是工作資料庫（FTS5 檢索），視為可拋棄；`app/replica.py` 把 users／sessions／audits／feedback／後台自訂知識壓成 gzip JSON 單列快照存 Postgres（預設每 120 秒、內容沒變不上傳、關機前補一次），開機時還原再重建索引。Zeabur 綁 PostgreSQL 服務即自動啟用（吃 `DATABASE_URL`／`POSTGRES_*`）；沒設定時完全 no-op、維持零依賴。取代原本「掛 Volume + APP_DB_PATH」方案。後台「系統健康」新增「Postgres 持久化」一項（會真的連線寫入一次快照）：未設定＝警告並提示重新部署會歸零、設定了但缺 psycopg 或連不上＝錯誤並顯示原因、正常＝顯示備份間隔與最後備份時間，不用翻 log。
- 月預算（`MONTHLY_BUDGET_TWD`）為硬限制：超標自動停用模型、降級抽取式（`model_status: budget_exhausted`）。
- 聊天限流 `CHAT_RATE_LIMIT_PER_MINUTE`（預設 20/分）。
- LLM timeout `LLM_TIMEOUT_SECONDS`（預設 60；曾因 20 秒太短導致推理模型全數降級「模型暫時無法完成生成」）。
- scrypt 參數 N=2^15/r=8/p=4（OWASP 等效、單次驗證 ~32MB），驗證在鎖外執行、入庫前鎖內重驗 hash。
- 稽核 log 的問題欄位自動遮罩 PII。
- loading 佔位訊息不落地：載入 localStorage 時過濾 `loading` 訊息（修復「重新整理後永遠轉圈」）。

## 已知待辦／觀察

- 2026-08-31：聊天已改串流輸出（/api/chat/stream，ndjson delta+result；/api/chat 保留為相容端點）。
- 2026-08-31：修復去識別化過度遮罩——coach 區塊由 knowledge/designer_coaching_process.md 重建；export_deploy_knowledge.py 加入角色詞/縮寫停用表、knowledge/* 來源不再重遮罩。歷史教材/案例的 [人名][歷史數值] 屬隱私設計，完整版需在使用者 Mac 重跑匯出或掛私人 KNOWLEDGE_JSONL。
- 2026-08-31：登入頁極簡化為 LOGO + "Your Private Brain" + 表單。

- LLM 降級除錯：Zeabur Log 搜 `[llm]` 看失敗階段與原因、搜 `[boot]` 看開機摘要（chunks 數可判斷正式站索引是否為最新版）。
- 待與使用者確認：正式站索引是否為 209 塊最新版、PWA 白色狀態列是否已重新加入主畫面圖示、missing_citations 是否還會出現。
- 未動工需求（使用者早前提出）：聊天可上傳圖片＋文字讓 AI 讀。
